# Rust
/backend/target
/cli/target
/backend/local_wealth.db
/backend/local_wealth.db-shm
/backend/local_wealth.db-wal
/backend/local_wealth.backup_*.db

# Encryption key files
backend/keyfile.local
/etc/wealthagent/keyfile

# Secrets — never commit any .env variant
.env
*.env
**/.env.local
**/.env.*.local

# Scratch / notes — treat as secret territory
docs/
**/*.local.md

# Production secrets — never commit real values
secrets/*
!secrets/.gitkeep
!secrets/README.md

# Built frontend assets (generated by ops/deploy.sh or docker buildx)
frontend-dist/

# Production env (copy from .env.production.example and fill in on VM)
.env.production

# Written on the server by ops/encrypt-pgdata.sh — machine-specific
docker-compose.override.yml

# OS
.DS_Store

# Editor
.idea/
.vscode/
*.swp
*.swo
